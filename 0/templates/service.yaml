apiVersion: v1
kind: Service
metadata:
  name: {{ printf "%s-workbench" .Values.prefix }}
  namespace: mariahi-lab-workbench
spec:
  type: ClusterIP
  ports:
    - port: 2222
      targetPort: 2222
  selector:
    app: mariahi-workbench